space ucl/convert/json

pull ucl/convert/dict

proc to_json(d Dict) -> String
  make out = "{"
  loop each e in d.entries
    out = out + "\""+e.key+"\""+":"+"\""+e.value+"\"","
  .end
  out = out + "}"
  ret out
.end

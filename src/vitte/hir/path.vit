space ucl/hir/path

form Path
  segments List of String
.end

proc to_string(p Path) -> String
  make out = ""
  loop each s in p.segments
    if out == ""
      out = s
    else
      out = out + "." + s
    .end
  .end
  ret out
.end

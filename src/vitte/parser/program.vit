space ucl/parser/program

pull ucl/parser
pull ucl/ast/program
pull ucl/ast/stmt

proc parse_program(p Parser) -> Program
  make items = []
  loop while p.pos < p.tokens.len
    make s = parse_statement(p)
    items.push(s)
  .end
  ret Program { items = items }
.end

space ucl/fs/paths

proc join(a String, b String) -> String
  if a == ""
    ret b
  .end
  ret a + "/" + b
.end

proc dirname(path String) -> String
  # simplified
  ret path
.end

proc basename(path String) -> String
  # simplified
  ret path
.end

space ucl/diag/error

pull ucl/diag/codes

form Error
  code String
  message String
  position Int
.end

proc new(code String, message String, position Int) -> Error
  ret Error { code = code, message = message, position = position }
.end

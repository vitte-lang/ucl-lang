space ucl/eval/scope

form Scope
  parent Scope?
  vars Map of String to String
.end

proc resolve(s Scope, key String) -> String
  if key in s.vars
    ret s.vars[key]
  .end
  if s.parent != null
    ret resolve(s.parent, key)
  .end
  ret ""
.end
